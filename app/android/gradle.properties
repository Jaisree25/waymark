org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Disable Kotlin incremental compilation: the pub cache (C:) and this project (D:)
# live on different drives, and Kotlin's incremental compiler cannot relativize
# paths across drive roots ("different roots" error). Harmless on single-filesystem
# CI (Codemagic). Remove if the pub cache and project ever share one drive.
kotlin.incremental=false
